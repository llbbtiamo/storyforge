spring:
  # 数据源
  datasource:
    url: jdbc:mysql://localhost:3306/storyforge?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Redis
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  # Mail
  mail:
    host: ${MAIL_HOST:smtp.example.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  # Spring AI - 平台默认模型配置（开发环境使用 DeepSeek 或 OpenAI 兼容接口）
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:sk-your-api-key}
      base-url: ${OPENAI_BASE_URL:https://api.openai.com}
      chat:
        options:
          model: ${OPENAI_MODEL:gpt-4o-mini}

# 认证配置
auth:
  mail-from: ${MAIL_FROM:no-reply@storyforge.local}

# JWT 配置
jwt:
  secret: ${JWT_SECRET:c3Rvcnlmb3JnZS1kZXYtc2VjcmV0LWtleS1wbGVhc2UtY2hhbmdlLWluLXByb2R1Y3Rpb24tZW52}
  expiration: 86400000  # 24 小时（毫秒）
  refresh-expiration: 604800000  # 7 天（毫秒）

# 日志级别
logging:
  level:
    com.storyforge: debug
    org.springframework.ai: debug
